/**
 * User Model
 */
var clout = require('clout-js'),
	Sequelize = clout.Sequelize,
	sequelize = clout.sequelize;

var definition = {
		name: { type: Sequelize.STRING, allowNull: false }
	},
	props = {};

module.exports = sequelize.define('Organization', definition, props);