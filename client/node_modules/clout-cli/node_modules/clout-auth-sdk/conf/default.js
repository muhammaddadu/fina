/**
 * Clout Auth Library
 */
module.exports = {
	env: process.env.NODE_ENV || 'production',
	baseUrl: 'http://auth.onestudiouk.com',
	paths: {
		login: ['POST', '/api/login'],
		logout: ['GET', '/api/logout'],
		whoami: ['GET', '/api/whoami']
	},
	session: ['connect.sid', 'express:sess', 'express:sess.sig']
};
